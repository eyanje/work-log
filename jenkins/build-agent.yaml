spec:
  containers:
    - image: harbor.eyanje.net/work-log/build-agent:latest
      name: jnlp
      volumeMounts:
        - mountPath: /jenkins/.local/share/containers
          name: work-log-build-cache
      securityContext:
        privileged: true
        runAsUser: 1000
  securityContext:
    hostNetwork: true
